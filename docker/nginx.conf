events {
    worker_connections 1024;
}

http {
    upstream api {
        server api:8000;
    }

    server {
        listen 80;
        server_name api.abdulghaffarmeatshop.com;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name api.abdulghaffarmeatshop.com;

        ssl_certificate /etc/letsencrypt/live/api.abdulghaffarmeatshop.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.abdulghaffarmeatshop.com/privkey.pem;

        location / {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /static {
            alias /app/static;
            expires 7d;
        }
    }
}
